import { Manrope } from "next/font/google";

// Typeface of the Job Editor Redesign, scoped to the job editor.
export const manrope = Manrope({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  display: "swap",
});
